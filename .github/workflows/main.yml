name: Test Internet

on:
  workflow_dispatch:

jobs:
  run-test:
    runs-on: ubuntu-latest
    steps:
      - name: Check Google
        run: curl -I https://google.com

      - name: Check Telegram API
        run: curl -I https://api.telegram.org

      - name: Print IP
        run: curl ifconfig.me
